/**
 * The fixed values `MainLayout` draws itself from.
 *
 * Plain re-export rather than the `export type` the sibling `types/` barrel
 * uses: this is a runtime value in a real `.ts` module.
 */
export { MAIN_LAYOUT_DEFAULTS } from './main-layout'
