/** The public type surface of `TaskDialog`. `export type` because the source is a `.d.ts`. */
export type {
  CategoryOption,
  TaskDialogEmits,
  TaskDialogExposed,
  TaskDialogMode,
  TaskDialogOpen,
  TaskDialogValues,
} from './task-dialog'
